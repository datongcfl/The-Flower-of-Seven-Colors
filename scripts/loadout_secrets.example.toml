# Agent 装备台 · 本地密钥样例
# 复制本文件为 loadout_secrets.toml，填入你的真实密钥（loadout_secrets.toml 不入库、被 gitignore）。
# 每个 [装备id] 下的 key 用于替换 catalog 中 url 的 {KEY} 占位符。
# 只有 requires_key=true 的装备才需要在这里出现。

[firecrawl]
key = "fc-你的真实key"

[tapd]
key = "你的tapd-key"
